import React from "react";
import styled from "styled-components/macro";

const HeaderWrapper = styled.h3`
  text-align: center;
`;

const Header = () => {
  return <HeaderWrapper>WORKING ON</HeaderWrapper>;
};

export default Header;
